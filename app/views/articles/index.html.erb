<h1>Oi mãe</h1>

<ul>
    <% @articles.each do | article | %>
        <li>
            <%= link_to article.title, article %> bonitão criado em <%= article.created_at%>
        </li>
    <%end%>    
</ul>
